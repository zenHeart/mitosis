{
  "mcpServers": {
    "playwright": {
      "command": "bash",
      "args": [
        "-c",
        "if [ -x \"$HOME/.claude/mcp-playwright-connect.sh\" ]; then exec \"$HOME/.claude/mcp-playwright-connect.sh\"; else exec npx -y @playwright/mcp@latest; fi"
      ]
    }
  }
}
